version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "<taskdef arn>"
        LoadBalancerInfo:
          ContainerName: "<alb name>"
          ContainerPort: 8080
